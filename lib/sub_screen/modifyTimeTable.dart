import 'package:flutter/widgets.dart';
import 'package:ngp/global.dart' as global;

class modifyTimetableUi extends StatelessWidget {
  
  @override
  Widget build( BuildContext context ) {
    return Text("Hey, just checking here.");
  }
}